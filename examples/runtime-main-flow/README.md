# Example: Runtime Main Flow

This example runs the core Session/Milestone/Plan/Execute flow using the default
components added to the framework. It uses a scripted model adapter that calls
one tool and then declares completion.

## Run

From the repository root:

```
python examples/runtime-main-flow/main.py
```

Expected output includes a success flag and a single milestone summary.
